<%= form_tag("/main_menu") do %>
<div>
	<% @basic_parameters.each_with_index do |item, index| %>
			<div><input type="text" name="<%=item["name"]%>"  placeholder="<%= item["description"]%>" class="rf"></div>
	<% end %>
	<%= submit_tag("Proceed") %>
</div>

<%end%>
